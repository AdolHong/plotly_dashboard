{
    "query": {
        "type": "SQL",
        "code": "SELECT * FROM sales LIMIT 10"
    },
    "visualization": [
        {
            "type": "python",
            "code": "# 返回表格\nresult = df"
        },
        {
            "type": "python",
            "code": "# 返回图\nimport plotly.express as px\nresult = px.bar(df.groupby(\"category\").sum().reset_index(), x=\"category\", y=\"price\")"
        }
    ]
}